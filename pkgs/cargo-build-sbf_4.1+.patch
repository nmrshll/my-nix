--- a/cargo-build-sbf/src/toolchain.rs
+++ b/cargo-build-sbf/src/toolchain.rs
@@ -129,6 +129,9 @@
 }
 
 pub fn make_platform_tools_path_for_version(version: &str) -> PathBuf {
+    if let Ok(dir) = std::env::var("SOLANA_PLATFORM_TOOLS_DIR") {
+        return PathBuf::from(dir);
+    }
     home_dir()
         .join(".cache")
         .join("solana")
